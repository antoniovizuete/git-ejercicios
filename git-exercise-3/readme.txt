Acabas de hacer commit de un fichero de backup: Main.java~

Los ficheros de backup no deben ser commiteados, vamos a corregir esto.

- Elimina el fichero
- Indexa los cambios, incluyendo los `git add -A`
- Escirbe un .gitignore que corrija el error
- Indexalo
- Haz commit.
