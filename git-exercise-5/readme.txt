Acabas de hacer `git fetch` y... Joffrey Baratheon ha tocado el mismo fichero
y la misma linea que tu...

Queires que el estado actual de to directorio de trabajo se guarde.

El conflicto parece inevitable.

- Crea un commit con la nueva version de first.txt
- Ejectuta `git pull` para fusionar origin\master en tu master.
- Resuelve el conflicto.
- Y `commitealo`